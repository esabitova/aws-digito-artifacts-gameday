# Copy that file to exec-vars.env and specify your values to avoid collisions between teammates
SERVICE=elasticache
TEST_COUNT=1
TEST_WORKERS=1
TEST_POOL_SIZE=ElasticacheClusterRedis=1
# Can be empty if you want to execute all tests
# TEST_TARGETS=
# For more that 1 test targets use space on the one line between them
TEST_TARGETS=documents/elasticache/sop/redis_increase_replica_count/2020-10-26/Tests/step_defs/test_redis_increase_replica_count.py