from publisher.src.parameter import Parameter

required_top_level_elements = ['description', 'assumeRole', 'parameters', 'mainSteps', 'schemaVersion']

required_parameters = [Parameter('AutomationAssumeRole', 'String', True)]

required_outputs = []

required_steps = []
